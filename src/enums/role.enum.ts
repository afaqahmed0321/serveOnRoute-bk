export enum ROLE {
  NORMAL_USER = 'user',
  RIDER = 'rider',
  ADMIN = 'admin',
  SUPER_ADMIN = 'super_admin',
  MANAGER = 'manager',
}

export enum Status{
  ACTIVE = 'active',
  BLOCKED = 'blocked' 
}